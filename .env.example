# Repo-root shared values.
#
# HOMELAB_IP is the host's LAN address — the one client machines point their
# /etc/hosts entries at for bookmark.home, minecraft.home, and media.example.lan.
HOMELAB_IP=192.0.2.10
