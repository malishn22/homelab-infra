services:
  jellyfin:
    container_name: "${JF_CONTAINER_NAME:-jellyfin}"
    image: jellyfin/jellyfin@sha256:aefb67e6a7ff1debdd154a78a7bbb780fd0c873d8639210a7f6a2016ad2b35db  # was jellyfin/jellyfin:latest, pinned 2026-09-10
    ports:
      - "${JF_HOST_PORT:-8096}:8096"
    volumes:
      - "${JF_CONFIG_DIR:-./config}:/config"
      - "${JF_CACHE_DIR:-./cache}:/cache"
      - "${JF_MEDIA_DIR:-/srv/media}:/media:ro"
    environment:
      JELLYFIN_PublishedServerUrl: "${JF_PUBLISHED_URL:-https://media.example.lan}"
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - "992"
      - "44"
    restart: unless-stopped
    networks:
      - homelab

networks:
  homelab:
    external: true
